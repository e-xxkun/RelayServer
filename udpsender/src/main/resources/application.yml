server:
  port: 8080
spring:
  redis:
    host: localhost
    database: 0
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 3000ms

#  rabbitmq:
#    host: localhost # rabbitmq????????
#    port: 5672 # rabbitmq?????????
#    virtual-host: /mall # rabbitmq??????host
#    username: mall # rabbitmq???????
#    password: xkk0512 # rabbitmq??????
#    publisher-confirms: true #?????????????????????????true

udp:
  store:
    UDP_MSG_BUFF_LEN: "UDP_MSG_BUFF_LEN"

# redis key
redis:
  key:
    prefix:
      REDIS_USER_SESSION_KEY : "REDIS_USER_SESSION_KEY"
      REDIS_USER_IDENTIFICATION_KEY : "REDIS_USER_IDENTIFICATION_KEY"
    expire:
      EXPIRE_TIME: 120


